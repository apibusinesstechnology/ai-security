config:
  target: "http://localhost:4000"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    metrics-by-endpoint: {}
  defaults:
    headers:
      Content-Type: "application/json"
  processor: "./dossier_test/load_test/processor.js"